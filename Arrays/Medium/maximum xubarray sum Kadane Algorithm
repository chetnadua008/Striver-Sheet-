//brute force
vector <int> temp;
for(int i=0;i<nums.size();i++){
    for(int j=i;j<nums.size();j++){
        int sum=0;
        for(int k=i;k<=j;k++){
            sum+=nums[k];
        }
        temp.push_back(sum);

    }
}
return *max_element(temp.begin(),temp.end());
